// Code generated by ent, DO NOT EDIT.

package lion

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/grpc-kit/pkg/lion/predicate"
	"github.com/grpc-kit/pkg/lion/usermemberships"
	"github.com/grpc-kit/pkg/lion/users"
)

// UserMembershipsQuery is the builder for querying UserMemberships entities.
type UserMembershipsQuery struct {
	config
	ctx           *QueryContext
	order         []usermemberships.OrderOption
	inters        []Interceptor
	predicates    []predicate.UserMemberships
	withLionUsers *UsersQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserMembershipsQuery builder.
func (_q *UserMembershipsQuery) Where(ps ...predicate.UserMemberships) *UserMembershipsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *UserMembershipsQuery) Limit(limit int) *UserMembershipsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *UserMembershipsQuery) Offset(offset int) *UserMembershipsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *UserMembershipsQuery) Unique(unique bool) *UserMembershipsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *UserMembershipsQuery) Order(o ...usermemberships.OrderOption) *UserMembershipsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryLionUsers chains the current query on the "lion_users" edge.
func (_q *UserMembershipsQuery) QueryLionUsers() *UsersQuery {
	query := (&UsersClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usermemberships.Table, usermemberships.FieldID, selector),
			sqlgraph.To(users.Table, users.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, usermemberships.LionUsersTable, usermemberships.LionUsersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserMemberships entity from the query.
// Returns a *NotFoundError when no UserMemberships was found.
func (_q *UserMembershipsQuery) First(ctx context.Context) (*UserMemberships, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usermemberships.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *UserMembershipsQuery) FirstX(ctx context.Context) *UserMemberships {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserMemberships ID from the query.
// Returns a *NotFoundError when no UserMemberships ID was found.
func (_q *UserMembershipsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usermemberships.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *UserMembershipsQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserMemberships entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserMemberships entity is found.
// Returns a *NotFoundError when no UserMemberships entities are found.
func (_q *UserMembershipsQuery) Only(ctx context.Context) (*UserMemberships, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usermemberships.Label}
	default:
		return nil, &NotSingularError{usermemberships.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *UserMembershipsQuery) OnlyX(ctx context.Context) *UserMemberships {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserMemberships ID in the query.
// Returns a *NotSingularError when more than one UserMemberships ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *UserMembershipsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usermemberships.Label}
	default:
		err = &NotSingularError{usermemberships.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *UserMembershipsQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserMembershipsSlice.
func (_q *UserMembershipsQuery) All(ctx context.Context) ([]*UserMemberships, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserMemberships, *UserMembershipsQuery]()
	return withInterceptors[[]*UserMemberships](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *UserMembershipsQuery) AllX(ctx context.Context) []*UserMemberships {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserMemberships IDs.
func (_q *UserMembershipsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(usermemberships.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *UserMembershipsQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *UserMembershipsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*UserMembershipsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *UserMembershipsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *UserMembershipsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("lion: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *UserMembershipsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserMembershipsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *UserMembershipsQuery) Clone() *UserMembershipsQuery {
	if _q == nil {
		return nil
	}
	return &UserMembershipsQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]usermemberships.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.UserMemberships{}, _q.predicates...),
		withLionUsers: _q.withLionUsers.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithLionUsers tells the query-builder to eager-load the nodes that are connected to
// the "lion_users" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserMembershipsQuery) WithLionUsers(opts ...func(*UsersQuery)) *UserMembershipsQuery {
	query := (&UsersClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLionUsers = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserMemberships.Query().
//		GroupBy(usermemberships.FieldCreatedAt).
//		Aggregate(lion.Count()).
//		Scan(ctx, &v)
func (_q *UserMembershipsQuery) GroupBy(field string, fields ...string) *UserMembershipsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserMembershipsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = usermemberships.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserMemberships.Query().
//		Select(usermemberships.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *UserMembershipsQuery) Select(fields ...string) *UserMembershipsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &UserMembershipsSelect{UserMembershipsQuery: _q}
	sbuild.label = usermemberships.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserMembershipsSelect configured with the given aggregations.
func (_q *UserMembershipsQuery) Aggregate(fns ...AggregateFunc) *UserMembershipsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *UserMembershipsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("lion: uninitialized interceptor (forgotten import lion/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !usermemberships.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("lion: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *UserMembershipsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserMemberships, error) {
	var (
		nodes       = []*UserMemberships{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withLionUsers != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserMemberships).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserMemberships{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withLionUsers; query != nil {
		if err := _q.loadLionUsers(ctx, query, nodes, nil,
			func(n *UserMemberships, e *Users) { n.Edges.LionUsers = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *UserMembershipsQuery) loadLionUsers(ctx context.Context, query *UsersQuery, nodes []*UserMemberships, init func(*UserMemberships), assign func(*UserMemberships, *Users)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserMemberships)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(users.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *UserMembershipsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *UserMembershipsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usermemberships.Table, usermemberships.Columns, sqlgraph.NewFieldSpec(usermemberships.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usermemberships.FieldID)
		for i := range fields {
			if fields[i] != usermemberships.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withLionUsers != nil {
			_spec.Node.AddColumnOnce(usermemberships.FieldUserID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *UserMembershipsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(usermemberships.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = usermemberships.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserMembershipsGroupBy is the group-by builder for UserMemberships entities.
type UserMembershipsGroupBy struct {
	selector
	build *UserMembershipsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *UserMembershipsGroupBy) Aggregate(fns ...AggregateFunc) *UserMembershipsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *UserMembershipsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserMembershipsQuery, *UserMembershipsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *UserMembershipsGroupBy) sqlScan(ctx context.Context, root *UserMembershipsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserMembershipsSelect is the builder for selecting fields of UserMemberships entities.
type UserMembershipsSelect struct {
	*UserMembershipsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *UserMembershipsSelect) Aggregate(fns ...AggregateFunc) *UserMembershipsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *UserMembershipsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserMembershipsQuery, *UserMembershipsSelect](ctx, _s.UserMembershipsQuery, _s, _s.inters, v)
}

func (_s *UserMembershipsSelect) sqlScan(ctx context.Context, root *UserMembershipsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
