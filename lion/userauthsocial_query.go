// Code generated by ent, DO NOT EDIT.

package lion

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/grpc-kit/pkg/lion/predicate"
	"github.com/grpc-kit/pkg/lion/userauthsocial"
)

// UserAuthSocialQuery is the builder for querying UserAuthSocial entities.
type UserAuthSocialQuery struct {
	config
	ctx        *QueryContext
	order      []userauthsocial.OrderOption
	inters     []Interceptor
	predicates []predicate.UserAuthSocial
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserAuthSocialQuery builder.
func (uasq *UserAuthSocialQuery) Where(ps ...predicate.UserAuthSocial) *UserAuthSocialQuery {
	uasq.predicates = append(uasq.predicates, ps...)
	return uasq
}

// Limit the number of records to be returned by this query.
func (uasq *UserAuthSocialQuery) Limit(limit int) *UserAuthSocialQuery {
	uasq.ctx.Limit = &limit
	return uasq
}

// Offset to start from.
func (uasq *UserAuthSocialQuery) Offset(offset int) *UserAuthSocialQuery {
	uasq.ctx.Offset = &offset
	return uasq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uasq *UserAuthSocialQuery) Unique(unique bool) *UserAuthSocialQuery {
	uasq.ctx.Unique = &unique
	return uasq
}

// Order specifies how the records should be ordered.
func (uasq *UserAuthSocialQuery) Order(o ...userauthsocial.OrderOption) *UserAuthSocialQuery {
	uasq.order = append(uasq.order, o...)
	return uasq
}

// First returns the first UserAuthSocial entity from the query.
// Returns a *NotFoundError when no UserAuthSocial was found.
func (uasq *UserAuthSocialQuery) First(ctx context.Context) (*UserAuthSocial, error) {
	nodes, err := uasq.Limit(1).All(setContextOp(ctx, uasq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userauthsocial.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) FirstX(ctx context.Context) *UserAuthSocial {
	node, err := uasq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserAuthSocial ID from the query.
// Returns a *NotFoundError when no UserAuthSocial ID was found.
func (uasq *UserAuthSocialQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uasq.Limit(1).IDs(setContextOp(ctx, uasq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userauthsocial.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) FirstIDX(ctx context.Context) int {
	id, err := uasq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserAuthSocial entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserAuthSocial entity is found.
// Returns a *NotFoundError when no UserAuthSocial entities are found.
func (uasq *UserAuthSocialQuery) Only(ctx context.Context) (*UserAuthSocial, error) {
	nodes, err := uasq.Limit(2).All(setContextOp(ctx, uasq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userauthsocial.Label}
	default:
		return nil, &NotSingularError{userauthsocial.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) OnlyX(ctx context.Context) *UserAuthSocial {
	node, err := uasq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserAuthSocial ID in the query.
// Returns a *NotSingularError when more than one UserAuthSocial ID is found.
// Returns a *NotFoundError when no entities are found.
func (uasq *UserAuthSocialQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uasq.Limit(2).IDs(setContextOp(ctx, uasq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userauthsocial.Label}
	default:
		err = &NotSingularError{userauthsocial.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) OnlyIDX(ctx context.Context) int {
	id, err := uasq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserAuthSocials.
func (uasq *UserAuthSocialQuery) All(ctx context.Context) ([]*UserAuthSocial, error) {
	ctx = setContextOp(ctx, uasq.ctx, "All")
	if err := uasq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserAuthSocial, *UserAuthSocialQuery]()
	return withInterceptors[[]*UserAuthSocial](ctx, uasq, qr, uasq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) AllX(ctx context.Context) []*UserAuthSocial {
	nodes, err := uasq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserAuthSocial IDs.
func (uasq *UserAuthSocialQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uasq.ctx.Unique == nil && uasq.path != nil {
		uasq.Unique(true)
	}
	ctx = setContextOp(ctx, uasq.ctx, "IDs")
	if err = uasq.Select(userauthsocial.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) IDsX(ctx context.Context) []int {
	ids, err := uasq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uasq *UserAuthSocialQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uasq.ctx, "Count")
	if err := uasq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uasq, querierCount[*UserAuthSocialQuery](), uasq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) CountX(ctx context.Context) int {
	count, err := uasq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uasq *UserAuthSocialQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uasq.ctx, "Exist")
	switch _, err := uasq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("lion: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uasq *UserAuthSocialQuery) ExistX(ctx context.Context) bool {
	exist, err := uasq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserAuthSocialQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uasq *UserAuthSocialQuery) Clone() *UserAuthSocialQuery {
	if uasq == nil {
		return nil
	}
	return &UserAuthSocialQuery{
		config:     uasq.config,
		ctx:        uasq.ctx.Clone(),
		order:      append([]userauthsocial.OrderOption{}, uasq.order...),
		inters:     append([]Interceptor{}, uasq.inters...),
		predicates: append([]predicate.UserAuthSocial{}, uasq.predicates...),
		// clone intermediate query.
		sql:  uasq.sql.Clone(),
		path: uasq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserAuthSocial.Query().
//		GroupBy(userauthsocial.FieldCreatedAt).
//		Aggregate(lion.Count()).
//		Scan(ctx, &v)
func (uasq *UserAuthSocialQuery) GroupBy(field string, fields ...string) *UserAuthSocialGroupBy {
	uasq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserAuthSocialGroupBy{build: uasq}
	grbuild.flds = &uasq.ctx.Fields
	grbuild.label = userauthsocial.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserAuthSocial.Query().
//		Select(userauthsocial.FieldCreatedAt).
//		Scan(ctx, &v)
func (uasq *UserAuthSocialQuery) Select(fields ...string) *UserAuthSocialSelect {
	uasq.ctx.Fields = append(uasq.ctx.Fields, fields...)
	sbuild := &UserAuthSocialSelect{UserAuthSocialQuery: uasq}
	sbuild.label = userauthsocial.Label
	sbuild.flds, sbuild.scan = &uasq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserAuthSocialSelect configured with the given aggregations.
func (uasq *UserAuthSocialQuery) Aggregate(fns ...AggregateFunc) *UserAuthSocialSelect {
	return uasq.Select().Aggregate(fns...)
}

func (uasq *UserAuthSocialQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uasq.inters {
		if inter == nil {
			return fmt.Errorf("lion: uninitialized interceptor (forgotten import lion/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uasq); err != nil {
				return err
			}
		}
	}
	for _, f := range uasq.ctx.Fields {
		if !userauthsocial.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("lion: invalid field %q for query", f)}
		}
	}
	if uasq.path != nil {
		prev, err := uasq.path(ctx)
		if err != nil {
			return err
		}
		uasq.sql = prev
	}
	return nil
}

func (uasq *UserAuthSocialQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserAuthSocial, error) {
	var (
		nodes = []*UserAuthSocial{}
		_spec = uasq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserAuthSocial).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserAuthSocial{config: uasq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uasq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uasq *UserAuthSocialQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uasq.querySpec()
	_spec.Node.Columns = uasq.ctx.Fields
	if len(uasq.ctx.Fields) > 0 {
		_spec.Unique = uasq.ctx.Unique != nil && *uasq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uasq.driver, _spec)
}

func (uasq *UserAuthSocialQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userauthsocial.Table, userauthsocial.Columns, sqlgraph.NewFieldSpec(userauthsocial.FieldID, field.TypeInt))
	_spec.From = uasq.sql
	if unique := uasq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uasq.path != nil {
		_spec.Unique = true
	}
	if fields := uasq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userauthsocial.FieldID)
		for i := range fields {
			if fields[i] != userauthsocial.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uasq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uasq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uasq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uasq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uasq *UserAuthSocialQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uasq.driver.Dialect())
	t1 := builder.Table(userauthsocial.Table)
	columns := uasq.ctx.Fields
	if len(columns) == 0 {
		columns = userauthsocial.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uasq.sql != nil {
		selector = uasq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uasq.ctx.Unique != nil && *uasq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uasq.predicates {
		p(selector)
	}
	for _, p := range uasq.order {
		p(selector)
	}
	if offset := uasq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uasq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserAuthSocialGroupBy is the group-by builder for UserAuthSocial entities.
type UserAuthSocialGroupBy struct {
	selector
	build *UserAuthSocialQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uasgb *UserAuthSocialGroupBy) Aggregate(fns ...AggregateFunc) *UserAuthSocialGroupBy {
	uasgb.fns = append(uasgb.fns, fns...)
	return uasgb
}

// Scan applies the selector query and scans the result into the given value.
func (uasgb *UserAuthSocialGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uasgb.build.ctx, "GroupBy")
	if err := uasgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserAuthSocialQuery, *UserAuthSocialGroupBy](ctx, uasgb.build, uasgb, uasgb.build.inters, v)
}

func (uasgb *UserAuthSocialGroupBy) sqlScan(ctx context.Context, root *UserAuthSocialQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uasgb.fns))
	for _, fn := range uasgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uasgb.flds)+len(uasgb.fns))
		for _, f := range *uasgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uasgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uasgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserAuthSocialSelect is the builder for selecting fields of UserAuthSocial entities.
type UserAuthSocialSelect struct {
	*UserAuthSocialQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uass *UserAuthSocialSelect) Aggregate(fns ...AggregateFunc) *UserAuthSocialSelect {
	uass.fns = append(uass.fns, fns...)
	return uass
}

// Scan applies the selector query and scans the result into the given value.
func (uass *UserAuthSocialSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uass.ctx, "Select")
	if err := uass.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserAuthSocialQuery, *UserAuthSocialSelect](ctx, uass.UserAuthSocialQuery, uass, uass.inters, v)
}

func (uass *UserAuthSocialSelect) sqlScan(ctx context.Context, root *UserAuthSocialQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uass.fns))
	for _, fn := range uass.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uass.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uass.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
