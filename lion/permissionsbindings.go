// Code generated by ent, DO NOT EDIT.

package lion

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/grpc-kit/pkg/lion/permissionsbindings"
)

// PermissionsBindings is the model entity for the PermissionsBindings schema.
type PermissionsBindings struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// 关联 lion_permissions 表的权限 ID
	PermissionID int `json:"permission_id,omitempty"`
	// 关联 lion_resource_scopes 表的资源 ID
	ResourceScopeID int `json:"resource_scope_id,omitempty"`
	selectValues    sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PermissionsBindings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case permissionsbindings.FieldID, permissionsbindings.FieldPermissionID, permissionsbindings.FieldResourceScopeID:
			values[i] = new(sql.NullInt64)
		case permissionsbindings.FieldCreatedAt, permissionsbindings.FieldUpdatedAt, permissionsbindings.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PermissionsBindings fields.
func (_m *PermissionsBindings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case permissionsbindings.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case permissionsbindings.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case permissionsbindings.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case permissionsbindings.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				_m.DeletedAt = new(time.Time)
				*_m.DeletedAt = value.Time
			}
		case permissionsbindings.FieldPermissionID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field permission_id", values[i])
			} else if value.Valid {
				_m.PermissionID = int(value.Int64)
			}
		case permissionsbindings.FieldResourceScopeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field resource_scope_id", values[i])
			} else if value.Valid {
				_m.ResourceScopeID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PermissionsBindings.
// This includes values selected through modifiers, order, etc.
func (_m *PermissionsBindings) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PermissionsBindings.
// Note that you need to call PermissionsBindings.Unwrap() before calling this method if this PermissionsBindings
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PermissionsBindings) Update() *PermissionsBindingsUpdateOne {
	return NewPermissionsBindingsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PermissionsBindings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PermissionsBindings) Unwrap() *PermissionsBindings {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("lion: PermissionsBindings is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PermissionsBindings) String() string {
	var builder strings.Builder
	builder.WriteString("PermissionsBindings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("permission_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.PermissionID))
	builder.WriteString(", ")
	builder.WriteString("resource_scope_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ResourceScopeID))
	builder.WriteByte(')')
	return builder.String()
}

// PermissionsBindingsSlice is a parsable slice of PermissionsBindings.
type PermissionsBindingsSlice []*PermissionsBindings
