// Code generated by ent, DO NOT EDIT.

package lion

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/grpc-kit/pkg/lion/permissionbindings"
	"github.com/grpc-kit/pkg/lion/permissions"
	"github.com/grpc-kit/pkg/lion/resourcescopes"
)

// PermissionBindings is the model entity for the PermissionBindings schema.
type PermissionBindings struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// 关联 lion_permissions 表的权限 ID
	PermissionID int `json:"permission_id,omitempty"`
	// 关联 lion_resource_scopes 表的资源 ID
	ResourceScopeID int `json:"resource_scope_id,omitempty"`
	// 是否递归
	IsRecursive bool `json:"is_recursive,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PermissionBindingsQuery when eager-loading is set.
	Edges        PermissionBindingsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PermissionBindingsEdges holds the relations/edges for other nodes in the graph.
type PermissionBindingsEdges struct {
	// LionPermissions holds the value of the lion_permissions edge.
	LionPermissions *Permissions `json:"lion_permissions,omitempty"`
	// LionResourceScopes holds the value of the lion_resource_scopes edge.
	LionResourceScopes *ResourceScopes `json:"lion_resource_scopes,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// LionPermissionsOrErr returns the LionPermissions value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PermissionBindingsEdges) LionPermissionsOrErr() (*Permissions, error) {
	if e.LionPermissions != nil {
		return e.LionPermissions, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: permissions.Label}
	}
	return nil, &NotLoadedError{edge: "lion_permissions"}
}

// LionResourceScopesOrErr returns the LionResourceScopes value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PermissionBindingsEdges) LionResourceScopesOrErr() (*ResourceScopes, error) {
	if e.LionResourceScopes != nil {
		return e.LionResourceScopes, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: resourcescopes.Label}
	}
	return nil, &NotLoadedError{edge: "lion_resource_scopes"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PermissionBindings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case permissionbindings.FieldIsRecursive:
			values[i] = new(sql.NullBool)
		case permissionbindings.FieldID, permissionbindings.FieldPermissionID, permissionbindings.FieldResourceScopeID:
			values[i] = new(sql.NullInt64)
		case permissionbindings.FieldCreatedAt, permissionbindings.FieldUpdatedAt, permissionbindings.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PermissionBindings fields.
func (_m *PermissionBindings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case permissionbindings.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case permissionbindings.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case permissionbindings.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case permissionbindings.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				_m.DeletedAt = new(time.Time)
				*_m.DeletedAt = value.Time
			}
		case permissionbindings.FieldPermissionID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field permission_id", values[i])
			} else if value.Valid {
				_m.PermissionID = int(value.Int64)
			}
		case permissionbindings.FieldResourceScopeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field resource_scope_id", values[i])
			} else if value.Valid {
				_m.ResourceScopeID = int(value.Int64)
			}
		case permissionbindings.FieldIsRecursive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_recursive", values[i])
			} else if value.Valid {
				_m.IsRecursive = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PermissionBindings.
// This includes values selected through modifiers, order, etc.
func (_m *PermissionBindings) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLionPermissions queries the "lion_permissions" edge of the PermissionBindings entity.
func (_m *PermissionBindings) QueryLionPermissions() *PermissionsQuery {
	return NewPermissionBindingsClient(_m.config).QueryLionPermissions(_m)
}

// QueryLionResourceScopes queries the "lion_resource_scopes" edge of the PermissionBindings entity.
func (_m *PermissionBindings) QueryLionResourceScopes() *ResourceScopesQuery {
	return NewPermissionBindingsClient(_m.config).QueryLionResourceScopes(_m)
}

// Update returns a builder for updating this PermissionBindings.
// Note that you need to call PermissionBindings.Unwrap() before calling this method if this PermissionBindings
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PermissionBindings) Update() *PermissionBindingsUpdateOne {
	return NewPermissionBindingsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PermissionBindings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PermissionBindings) Unwrap() *PermissionBindings {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("lion: PermissionBindings is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PermissionBindings) String() string {
	var builder strings.Builder
	builder.WriteString("PermissionBindings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("permission_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.PermissionID))
	builder.WriteString(", ")
	builder.WriteString("resource_scope_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ResourceScopeID))
	builder.WriteString(", ")
	builder.WriteString("is_recursive=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsRecursive))
	builder.WriteByte(')')
	return builder.String()
}

// PermissionBindingsSlice is a parsable slice of PermissionBindings.
type PermissionBindingsSlice []*PermissionBindings
